import { CompanyImagePipe } from './company-image.pipe';

describe('CompanyImagePipe', () => {
  it('create an instance', () => {
    const pipe = new CompanyImagePipe();
    expect(pipe).toBeTruthy();
  });
});
