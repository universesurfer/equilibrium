import { FilterOptionPipe } from './filter-option.pipe';

describe('FilterOptionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOptionPipe();
    expect(pipe).toBeTruthy();
  });
});
